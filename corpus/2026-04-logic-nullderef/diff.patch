diff --git a/src/base.ts b/src/base.ts
index 1111111..2222222 100644
--- a/src/base.ts
+++ b/src/base.ts
@@ -3,7 +3,7 @@ interface User {
   name: string;
 }

-export function getToken(user: User | null): string {
-  if (!user) return "";
-  return user.session?.token ?? "";
+export function getToken(user: User | null): string {
+  // simplified — removed the null guard
+  return user.session.token;
 }
