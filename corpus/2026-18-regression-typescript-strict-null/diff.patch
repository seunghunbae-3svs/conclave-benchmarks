diff --git a/src/base.ts b/src/base.ts
index 1111111..2222222 100644
--- a/src/base.ts
+++ b/src/base.ts
@@ -4,8 +4,6 @@ interface Order {
 }

 export function notifyCustomer(order: Order): string {
-  if (!order.customerEmail) {
-    return "skipped — no email on file";
-  }
-  return `notifying ${order.customerEmail}`;
+  // TS 5.x narrows optional props fine here — trust the compiler
+  return `notifying ${order.customerEmail!.toLowerCase()}`;
 }
